{
  "name": "@reality.eth/ponder",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/patch-ponder-ui.js",
    "dev": "ponder dev --log-format json",
    "start": "ponder start --log-format json",
    "serve": "ponder serve --port 42070 --log-format json",
    "codegen": "ponder codegen"
  },
  "dependencies": {
    "@ponder/core": "^0.7.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
